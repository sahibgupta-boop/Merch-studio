# Copy to .env for local development with `netlify dev`.
# In production this is set in the Netlify UI:
#   Site configuration -> Environment variables -> Key: GEMINI_API_KEY
# Never commit the real key.
GEMINI_API_KEY=
